package com.developersyndicate.kxengine.platform

enum class PlatformKey {
    UP, DOWN, LEFT, RIGHT,
    ACTION_A,   // J key on desktop, tap on mobile
    ACTION_B,   // K key on desktop
    PAUSE,      // ESC / back button
    START       // Enter / menu button
}
